spring.application.name=cloudApiLayer

# Run this API layer on port 8081
server.port=8081

# Base URL for the legacy reservation system (update if needed)
legacy.system.base-url=http://localhost:8080

# Base URL for the recommendation engine
recommendation.engine.base-url=http://localhost:8082

# JWT Configuration - Use environment variable for production
jwt.secret=${JWT_SECRET:dev_secret_key_12345_very_long_for_development_only_change_in_production}
